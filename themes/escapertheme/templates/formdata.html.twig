{% extends 'partials/base.html.twig' %}

{% block content %}
<div class="uk-container uk-container-center uk-height-viewport uk-flex uk-flex-middle">
    <div class="uk-width-1-1">
        <div class="uk-text-center">{{ content }}</div>

        <div class="uk-alert uk-alert-success">{{ form.message }}</div>

        {% include "forms/data.html.twig" %}
    </div>
</div>
{% endblock %}
